# Finora v2.0.9 — Finsights UI Refinement

Upload the contents of this folder to the GitHub repository root.

Open with a cache-busting URL after deployment:

```text
https://jndesignlab-cloud.github.io/Finora-DesignLab/?v=2.0.9
```

Use Ctrl + Shift + R after uploading. The service worker cache name was updated to `finora-supabase-v2.0.9`.

Default personal login remains:

```text
Username: jaravata
Password: atavaraj
```

The session is stored only in `sessionStorage`, so closing/resetting the browser session logs the user out.
